# 期末試験対策(基礎理解編)：解答解説編

以下では、$\log x$ は自然対数、すなわち底が $e$ の対数を表す。また、$e$の近似値を用いた小数計算は行わず、答えは $e$ を含む形で表してよい。

## 第1問：指数と対数の計算

```{admonition}問題
(1) 次の指数方程式を解きなさい。
$$
2^x = \frac{1}{4}
$$
```
**解答解説**

指数方程式では、**左辺と右辺の底と指数を揃える**ことが重要。そのとき、指数の法則と特徴を活用する。この作業を行わないと指数方程式は解けない。むしろ、この作業を行うだけで、指数方程式は解ける。

指数の特徴を用いて、右辺を底が2になるように変形しよう。
$$
\frac{1}{4} = \frac{1}{2^2} = 2^{-2}
$$
よって、指数方程式は
$$
2^x = 2^{-2}
$$
と書けるから、$x=-2$となる。

---
```{admonition}問題
(2) 次の対数方程式を解きなさい。
$$
\log_x 10000 = 2
$$
```
**解答解説**

対数方程式では、対数に関連する用語：**底・真数・対数**を常に把握することが重要。「**底**を**対数**回掛ける(**対数**乗する)と、**真数**になる」が対数の定義である。

上の式は、「**x**を**2**回掛けると、**10000**になる」ことを表している。2乗して10000になる数は100であるから、$x=100$が答え。

**[別解]** 対数の性質を使って、左辺を
$$
\log_x 10000 = \log_x 100^2 = 2\log_x 100 
$$
と整理すれば、上の対数方程式は
$$
2\log_x 100 =2\iff \log_x 100 = 1
$$
と変形できる。この式は$x^1=100$を意味しているから、$x=100$。

---
```{admonition}問題
(3) $a,b,c$を1より大きい数とするとき、次の式を簡単にしなさい。
$$
\log_a b\times \log_b c\times \log_c a
$$
```
**解答解説**

底が異なる対数の式に対しては、**とにかく底を揃えること**が第一歩。
揃える底は、なんでもよい。ここでは底を「2」で揃えよう。

底を揃えるには、**底の変換公式**を使うことが定石。
掛け算している3つの項をそれぞれ底2に変換すると：
$$
\log_a b = \frac{\log_2b}{\log_2a},\,
\log_b c = \frac{\log_2c}{\log_2b},\,
\log_c a = \frac{\log_2a}{\log_2c}
$$
となる。
よって、
$$
\log_a b\times \log_b c\times \log_c a = \frac{\log_2b}{\log_2a} \times \frac{\log_2c}{\log_2b} \times \frac{\log_2a}{\log_2c} = 1
$$
が答え。

---

## 第2問:微分の計算
```{admonition}問題
(4) 次の式を微分しなさい。
$$
3x^2 + 2x + 1
$$
```

**解答解説**

整関数の導関数を求める公式：$(x^n)'=nx^{n-1}$を使う。

$f(x) = 3x^2 + 2x+1$を微分すると、
$$
f'(x) = 3\times 2x+ 2\times 1 + 0= 6x+2
$$

---
```{admonition}問題
(5) 次の式を微分しなさい。
$$
\log x + \frac{1}{2}x^2
$$
```

**解答解説**

ネイピア数$e$を底とする対数の導関数を求める公式：$(\log x)'=\frac{1}{x}$を使う。

$f(x) = \log x + \frac{1}{2}x^2$を微分すると、
$$
f'(x) = \frac{1}{x} + \frac{1}{2}\times 2x=\frac{1}{x}+x
$$

---
```{admonition}問題
(6) 次の式を微分しなさい。
$$
2^x
$$
```
**解答解説**

指数の導関数を求める一般的な公式：$(a^x)'=a^x\log a$を使う。

$f(x) = 2^x$を微分すると、
$$
f'(x) = 2^x\log 2
$$

---
```{admonition}問題
(7) 次の式を微分し、簡単にしなさい。
$$
\frac{3^x}{\log 9}
$$
```
**解答解説**

底が$a$の指数関数の導関数の公式：$(a^x)'=a^x\log a$を使う。微分した式に対数の性質を使い、式をさらに簡単にする。

$f(x) = \frac{3^x}{\log 9}$を微分すると、
$$
f'(x) = \frac{3^x\log 3}{\log 9}
$$
ここで、$\log 9$は$\log 3^2$である。対数の性質から$\log 3^2=2\log 3$であるから、
$$
f'(x) = \frac{3^x\log 3}{\log 9} = \frac{3^x\log 3}{2\log 3} = \frac{3^x}{2}
$$

---

```{admonition}問題
(8) 合成関数の微分の公式を使って、次の式を微分しなさい。

$$
(3-2x)^3
$$
```

**解答解説**

$f(x) = (3-2x)^3$と設定する。この関数は**どんな関数を合成したものか**を明らかにすることが最初のステップである。明らかにする方法は、授業でも説明した通り、具体的な入力$x$を関数$f$に与えた時に得られる出力$f(x)=y$を考えることである。

1. $x$に特定の値を設定
2. その$x$を使って、まずは$3-2x$を計算
3. $3-2x$の値を$z$という一時的な出力に設定
4. $z$を使って$z^3$を計算
5. $z^3$の値が最終出力$y$となる。

この流れを理解したら、2.と4.の計算部分を2つの関数$g(x),h(z)$に置き換える。
1. 入力値$=x$
2. $g(x) = 3-2x$
3. $z = g(x)$
4. $h(z)=z^3$
5. $z^3 = y$

すると、関数$f(x)$は、$z=g(x)=3-2x$を入力とする関数$h(z)$から最終出力$y=h(z)=z^3$を得るという処理の流れを表している。これが「合成」関数$h(g(x))$である。
$$
f(x) = h(g(x))
$$

合成関数の微分公式
$$
f'(x)=h'(z)g'(x)
$$
を用いると、
$$
f'(x) =　(z^3)'(3-2x)' = 3z^2\times(-2)
$$
となる。最後に$z=3-2x$を代入すれば、
$$
f'(x) = 3(3-2x)^2\times (-2) = -6(3-2x)^2
$$
となる。


```{admonition}問題
(9) 関数の積の微分の公式を使って、次の式を微分しなさい。
$$
(x-1)e^x 
$$
```

**解答解説**

ある関数$f(x)$が、2つの関数$g(x),\,h(x)$の積：
$$
f(x)\equiv g(x)\times h(x)
$$
とみなせるとき、積の微分の公式
$$
f'\equiv (g\times h)' = g'h + hg'
$$
を適用する問題である。

$f(x)=(x-1)e^{x}$とおくと、この関数は2つの関数
$$
g(x) = x-1,\,h(x)=e^x
$$
の積とみなせる。よって、
$$
\begin{aligned}
f'(x)=(x-1)'e^x + (x-1)(e^x)'&= 1\times e^x + (x-1)e^x \\
&=  e^x + xe^x-e^x  \\
&= xe^x
\end{aligned}
$$

---
```{admonition}問題
(10) 次の式を微分しなさい。ただし、関数の積の微分の公式と合成関数の微分の公式を1回ずつ使い、最後は因数分解してまとめなさい。
```

$$
xe^{x^2}
$$

**解答解説**

$f(x)=xe^{x^2}$とおく。$f(x) = x\times e^{x^2}$と見れば、関数の積の微分が使える。
$$
\begin{aligned}
f'(x) &= (x)'\times (e^{x^2}) + x\times (e^{x^2})' \\
&= e^{x^2} + x(e^{x^2})'
\end{aligned}
$$
残りは$e^{x^2}$の導関数を計算するだけである。この式の計算プロセスを考えると、

1. 入力$x$を設定
2. まず$z=x^2$を計算。
3. $z$を使って$e^z$を計算。
4. $e^z$が最終出力$y$になる。

となる。2.を$z=g(x)=x^2$、3.を$y=h(z)=e^z$という関数の合成関数と捉えると、
$$
\begin{aligned}
(e^{x^2})' &= h'(z)g'(x) \\
&= (e^z)'(x^2)'\\
&= e^z\times 2x
\end{aligned}
$$
最後に、$z=x^2$を代入して、$(e^{x^2})'=e^{x^2}2x$となる。

以上をまとめると、$f(x)$の導関数は
$$
\begin{aligned}
f'(x) &= e^{x^2} + x(e^{x^2})' \\
&= e^{x^2} + x(e^{x^2}2x) = e^{x^2} +2x^2e^{x^2} \\
&= (1+2x^2) e^{x^2}
\end{aligned}
$$
となる。

```{note}

合成関数の微分にもう少し慣れておきたい人へのおすすめ問題2選。

「n乗」「指数」を使った合成関数の微分の代表的な問題が、上の3つ問題。これらの他よく出題されるのは「分数」「対数」を使った合成関数の問題。

1. $f(x) = \frac{1}{1+e^{-x}}$の導関数$f'(x)$を求めなさい。
    * 「分数」と「$1+e^{-x}$」の合成関数
    * **正答** $\frac{e^{-x}}{(1+e^{-x})^2}$
        * この$f(x)$は「ロジスティック関数」と呼ばれ、機械学習やAIで広く利用される。
2. $f(x) = \log (2x^2 +3x)$の導関数$f'(x)$を求めなさい。
    * 「対数」と「$2x^2+3x$」の合成関数
    * **正答** $\frac{4x+3}{2x^2 +3x}$
```

---

## 第3問:極値を求める

```{important}
**極値・最大最小を求める手順**

1. 一階導関数 $f'(x)$ を求める。
2. $f'(x)=0$ を解き、極値の**候補**を求める。
3. 二階導関数 $f''(x)$ を求める。
4. $f''(x)$ の符号から判定する。
    * $f''(x)>0$ ：下に凸 → 極小値
    * $f''(x)<0$ ：上に凸 → 極大値
5. 必要に応じて関数値を計算する。
```

```{admonition}問題
(11) $f(x) = x\log x$の最小値を求めなさい。ただし、$x>0$とする。
```
**解答解説**

導関数$f'(x)$は
$$
f'(x) =(x)'\log x+x(\log x)' = \log x + x\times\frac{1}{x} = \log x + 1
$$
となる。

$f'(x)=0$を解くと、

$$
\log x + 1 = 0\iff \log x = -1 \iff x = e^{-1}
$$
(対数方程式を解くときには、必ず対数の定義を思い出そう。上の式は、ネイピア数$e$の-1乗が真数$x$であるという意味である。)

二階導関数$f''(x)$を計算する。
$$
f''(x) = (\log x + 1)' = \frac{1}{x}
$$


$x>0$なので、$\frac{1}{x}$の値は常に正である。よって、$f''(x)>0$となり、$f(x)$は下に凸である。よって、極小値が存在する。

極小値を求めると、
$$
f(e^{-1}) = e^{-1}\log e^{-1} = -e^{-1}
$$
この極小値が最小値である。

---
```{admonition}問題
(12) $f(x) = x^3-4x^2$は$x = \frac{8}{3}$で極小値をとることを、二階導関数$f''(x)$を使って説明しなさい。
```
**解答解説**

導関数$f'(x)$を計算する。
$$
f'(x) = 3x^2 -8x
$$

極値は$f'(x)=0$を満たす$x$によって与えられる。
$$
f'(x) = 0 \iff 3x^2-8x=0\iff x(3x-8)=0
$$
この二次方程式の解は2つ存在し、$x=0,\frac{8}{3}$である。それぞれの解について、極大値/極小値の判定を行う。

まず、二階導関数は
$$
f''(x) = (3x^2-8x)' = 6x-8
$$
である。

$x=0$のときを調べる。
* $f''(0)=6\times 0-8=-8<0$なので、関数$f(x)$は上に凸である。
* よって、$x=0$のときの関数の値$f(0)$は極大値である。

次に、$x=\frac{8}{3}$のときを調べる。
* $f''(\frac{8}{3})=6\times \frac{8}{3}-8=16-8>0$なので、$x=\frac{8}{3}$では、関数$f(x)$は下に凸である。

以上より、$x=\frac{8}{3}$のときの関数の値$f(\frac{8}{3})$は、極小値である。

---
```{admonition}問題
(13) $f(x) = xe^{-x}$ の極値を与える$x$を計算しなさい。その値を$x^*$とすると、$f(x^*)$は極小値か極大値か、判定しなさい。
```
**解答解説**

導関数$f'(x)$を計算する。関数の積の微分公式を使う。
$$
\begin{aligned}
f'(x) &= (xe^{-x})' \\
&= (x)'e^{-x}+x(e^{-x})' \\
&= e^{-x} -x e^{-x} = (1-x)e^{-x}
\end{aligned}
$$

次に、$f'(x)=0$を解く。
$$
\begin{aligned}
f'(x)=0 \iff (1-x)e^{-x} = 0
\end{aligned}
$$
指数関数の性質より$e^{-x}$は常に正の値であるから、$f'(x)=0$を満たす$x$は$1$のみである。よって、$x^*=1$。

次に二階導関数を計算する。
$$
\begin{aligned}
f''(x) &= (e^{-x}(1-x))' \\
&= (e^{-x})'(1-x)+e^{-x}(1-x)' \\
&= -e^{-x}(1-x)+e^{-x}\times (-1) \\
&= -e^{-x} + xe^{-x} -e^{-x} \\
&= -2e^{-x}+xe^{-x} \\
&= (x-2)e^{-x}
\end{aligned}
$$

$x=x^*$のときの二階導関数の符号を調べる。
$$
f''(x^*) = f''(1) = (1-2)e^{-1} = -e^{-1}< 0
$$

よって、$x=1$のとき関数は上に凸である。
以上より、関数$f(x)$は、$x=1$で極大値をもつ。

---

## 第4問:一次近似式
```{admonition}問題
(14) 関数 $f(x) = \sqrt{x}\,(x>0)$の、$x=4$における一次近似式を導出しなさい。
```

**解答解説**

$f(x)$の$x=a$における一次近似式は、
$$
f'(a)(x-a) + f(a)
$$
であるので、$x=4$における一次近似式は$a=4$を代入して、
$$
f'(4)(x-4) + f(4)
$$
である。ここで
$$
f'(x) = (\sqrt{x})' = \left(x^{\frac{1}{2}}\right)' = \frac{1}{2}x^{-\frac{1}{2}}
$$
なので、$f'(4) = \frac{1}{2}4^{-1/2}=\frac{1}{2}\frac{1}{\sqrt{4}}=\frac{1}{4}$ともとまる。

また、$f(4) = \sqrt{4}=2$である。以上より一次近似式は
$$
\frac{1}{4}(x-4) + 2 = \frac{1}{4}x-1+2=\frac{1}{4}x+1
$$

---
```{admonition}問題
(15) (14)の結果を用いて、$\sqrt{4.02}$の近似値を求めなさい。
```

**解答解説**

$$
\frac{1}{4}\times 4.02+1 = 1.005 + 1 = 2.005
$$

ちなみにExcelで$\sqrt{4.02}$を厳密に計算すると、$2.004993766$という値である。よって、上で求めた$2.005$は確かに近似値となっていることがわかる。

<small>
(余談) スマホの顔認証技術や生成AIの回答を作るプロセスでは、顔の表情や質問文を内部で大量に一次近似して、高精度の近似値を一気に計算する。一次近似式=一次関数をまとめて計算する手法は「線形代数」と呼ばれ、秋学期「データサイエンス基礎2」のメインの内容である。
</small>

## 第5問：最適化問題
```{admonition}問題
(16) 損失関数$L$を
$$
L(a,b) = (1-b)^2+(2-a-b)^2+(2-2a-b)^2
$$
と定義する。$L(a,b)$を$a$と$b$についてそれぞれ偏微分し、損失関数を最小にする$a,b$が満たす連立方程式を導出しなさい。
```
**解答解説**

```{important}
1. 2つの入力$a,b$をもつ損失関数$L(a,b)$について、2つの偏導関数を計算する。
2. 2つの偏導関数=0とおくと、$a,b$に関する2元連立方程式が得られる。
$$
\frac{\partial L}{\partial a} = 0,\,\frac{\partial L}{\partial b} = 0
$$
```
$$
\begin{aligned}
\frac{\partial L}{\partial a} &= 2(2-a-b)\times (-1)+ 2(2-2a-b)\times (-2)\\
& = (-4+2a+2b)+(-8+8a+4b) \\
& = 10a +6b-12 \\
\frac{\partial L}{\partial b} &= 2(1-b)\times (-1)+ 2(2-a-b)\times (-1) + 2(2-2a-b)\times (-1)\\
& = (-2+2b) + (-4+2a+2b) + (-4+4a+2b) \\
& = 6a + 6b -10
\end{aligned}
$$

$$
\begin{cases}
10a + 6b -12 = 0 & (1)\\
6a + 6b -10 = 0 &(2)
\end{cases}
$$

---
```{admonition}問題
(17) (16)の解を求めなさい。
```

**解答解説**

2元連立方程式を解く。ここでは消去法を使って$b$を消去して$a$を先に求める。$(1)-(2)$を計算すると、

$$
4a-2=0\iff a = \frac{1}{2}
$$

次に$b$を求める。$a=\frac{1}{2}$を(2)式に代入すると

$$
3 + 6b-10 = 0 \iff 6b = 7 \iff b = \frac{7}{6}
$$

以上より、$a = \frac{1}{2},\,b=\frac{7}{6}$。

## 第6問: 積分
```{admonition}問題
(18) $f(x)=x^2-4x-9$とするとき、次の定積分を求めなさい。
$$
\int_{2}^{4} f(x) dx
$$
```

**解答解説**

$x$の$n$乗 $x^n$ の原始関数は $\frac{1}{n+1}x^{n+1}$ で表されるから、

* 微分して$x^2$になる関数は？→ $\frac{1}{3}x^{3}$
* 微分して$-4x$になる関数は？→ $-2x^2$
* 微分して$-9$になる関数は？→ $-9x$

以上より、$f(x)$の原始関数は
$$
F(x) = \frac{1}{3}x^3 -2x^2 -9x
$$
となる。よって、定積分の値は$F(4)-F(2)$で計算すればよい。
$$
\begin{aligned}
F(4)-F(2) &= \left(\frac{1}{3}4^3-2\times 4^2 -9\times 4\right)-\left(\frac{1}{3}2^3-2\times 2^2 -9\times 2\right) \\
&= \left(\frac{64}{3}-32-36\right)-\left(\frac{8}{3}-8-18\right)\\
&=-\frac{140}{3}-\left(-\frac{70}{3}\right) = -\frac{70}{3}
\end{aligned}
$$

--- 
```{admonition}問題
(19) $f(x)=e^{x}+1$とするとき、次の定積分を求めなさい。
$$
\int_{0}^{4}f(x)dx
$$
```
**解答解説**

* 微分して$e^x$となる関数は？→ $e^x$
* 微分して$1$となる関数は？→ $x$

以上より、$f(x)$の原始関数は

$$
F(x) = e^x + x
$$

定積分の値は、$F(4)-F(0)$を計算して求める。

$$
\begin{aligned}
F(4)-F(0) &= (e^4+4) - (e^0+0) \\
&= e^4+4-1\\
& = e^4+3
\end{aligned}
$$

---
```{admonition}問題
(20) 確率変数$X$について、累積分布関数$F(x)$ が $F(10)=0.98$、$F(3)=0.22$であるとき、次の確率を求めなさい。
$$
P(3\le X\le 10)
$$
```
**解答解説**

範囲の確率は、「範囲の上端以下の確率」ー「範囲の下端以下の確率」で計算できるから、

$$
P(3\le X\le 10) = P(X\le 10) - P(X\le 3) = 0.98-0.22 = 0.76
$$
