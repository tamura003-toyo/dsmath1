# 期末試験対策(基礎理解編)

以下では、$\log x$ は自然対数、すなわち底が $e$ の対数を表す。また、$e$の近似値を用いた小数計算は行わず、答えは $e$ を含む形で表してよい。

## 第1問：指数と対数の計算
```{note}
(1)(2)は指数と対数の定義が理解できているかをチェックする問題。(3)は底の変換公式の解き方を身につけているかをチェックする問題。
```

(1) 次の指数方程式を解きなさい。
$$
2^x = \frac{1}{4}
$$

(2) 次の対数方程式を解きなさい。
$$
\log_x 10000 = 2
$$

(3) $a,b,c$を1より大きい数とするとき、次の式を簡単にしなさい。
$$
\log_a b\times \log_b c\times \log_c a
$$

## 第2問:微分の計算
```{note}
(4)-(6)は微分の公式、(7)は微分の公式と対数の性質を組み合わせる問題。(8)-(10)は問題文に書いてある通りの内容をチェックする問題。
```

(4) 次の式を微分しなさい。
$$
3x^2 + 2x + 1
$$

(5) 次の式を微分しなさい。
$$
\log x + \frac{1}{2}x^2
$$

(6) 次の式を微分しなさい。
$$
2^x
$$
(7) 次の式を微分し、簡単にしなさい。
$$
\frac{3^x}{\log 9}
$$

(8) 合成関数の微分の公式を使って、次の式を微分しなさい。

$$
(3-2x)^3
$$

(9) 関数の積の微分の公式を使って、次の式を微分しなさい。

$$
(x-1)e^x 
$$

(10) 次の式を微分しなさい。ただし、関数の積の微分の公式と合成関数の微分の公式を1回ずつ使い、最後は因数分解してまとめなさい。

$$
xe^{x^2}
$$

## 第3問:極値を求める
```{note}
(11)(13)は積の微分の公式を使って解く問題。(12)(13)は二階導関数の符号と極大極小判定を結びつける問題。
```

(11) $f(x) = x\log x$の最小値を求めなさい。ただし、$x>0$とする。

(12) $f(x) = x^3-4x^2$は$x = \frac{8}{3}$で極小値をとることを、二階導関数$f''(x)$を使って説明しなさい。

(13) $f(x) = xe^{-x}$ の極値を与える$x$を計算しなさい。その値を$x^*$とすると、$f(x^*)$は極小値か極大値か、判定しなさい。

## 第4問:一次近似式
```{note}
(14)は一次近似式の定式化がちゃんとできるかチェックする問題。(15)は一次近似を使えば、面倒な根号の値がささっと解けることを体験する問題。
```

(14) 関数 $f(x) = \sqrt{x}\,(x>0)$の、$x=4$における一次近似式を導出しなさい。

(15) (14)の結果を用いて、$\sqrt{4.02}$の近似値を求めなさい。

## 第5問：最適化問題
```{note}
最小二乗法を解く道筋をちゃんとフォローできるかチェックする問題。
```

(16) 損失関数$L$を
$$
L(a,b) = (1-b)^2+(2-a-b)^2+(2-2a-b)^2
$$
と定義する。$L(a,b)$を$a$と$b$についてそれぞれ偏微分し、損失関数を最小にする$a,b$が満たす連立方程式を導出しなさい。

(17) (16)の解を求めなさい。

## 第6問: 積分
```{note}
(18)(19)は原始関数を使って積分計算ができるかどうかチェックする問題。(20)は積分による確率の計算公式(3つ)を適用できるかチェックする問題。
```

(18) $f(x)=x^2-4x-9$とするとき、次の定積分を求めなさい。
$$
\int_{2}^{4} f(x) dx
$$

(19) $f(x)=e^{x}+1$とするとき、次の定積分を求めなさい。
$$
\int_{0}^{4}f(x)dx
$$

(20) 確率変数$X$について、累積分布関数$F(x)$ が $F(10)=0.98$、$F(3)=0.22$であるとき、次の確率を求めなさい。
$$
P(3< X\le 10)
$$
